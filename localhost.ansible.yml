---

- hosts: localhost
  connection: local

  vars:
    git_conf:
      - name: user.name
        value: Fly me to the moon
      - name: user.email
        value: git@bigly.dog

    sync_all: "{{ all | default(False) | bool }}"
    from_scratch: "{{ reset | default(False) | bool }}"

  tasks:
    - assert:
        that: ansible_distribution == 'MacOSX'

    - import_role:
        name: _unix
    - import_role:
        name: macos
