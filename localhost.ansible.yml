---

- hosts: localhost
  connection: local

  vars:
    git_conf:
      - name: user.name
        value: Fly me to the moon
      - name: user.email
        value: git@bigly.dog

    sync_all: no
    from_scratch: no

  tasks:
    - assert:
        that: ansible_distribution == 'MacOSX'

    - import_role:
        name: macos
