---

- name: "install python libs {{ python_packages }}"
  become: "{{ ansible_distribution != 'MacOSX' }}"
  pip:
    executable: "{{ item.version }}"
    name: "{{ item.name }}"
  loop: "{{ python_packages }}"
