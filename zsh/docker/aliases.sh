#!/usr/bin/env -S -- bash

alias -- dr='docker run --rm --interactive --tty'
alias -- dre='dr --entrypoint'
alias -- dex='docker exec --interactive --tty --user 0'
alias -- doc='docker compose'
alias -- dlog='docker logs --follow'
