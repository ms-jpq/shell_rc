#!/usr/bin/env -S -- bash

setopt -- interactivecomments # allow comments in prompt
setopt -- multios             # auto tee
setopt -- no_flowcontrol      # Turn off flow control characters in readline
setopt -- prompt_subst        # auto expansion at commandline
