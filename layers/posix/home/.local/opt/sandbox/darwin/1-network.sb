(version 1)

(allow network*)
; (allow system-socket)

(allow file-read*
  (literal "/private/var/run/resolv.conf"))

(allow file-read*
  (subpath (xdg-config "curl")
           (xdg-config "wget")))
