#cloud-config
---
bootcmd:
  - - cloud-init-per
    - once
    - uuid-0
    - date
    - "--set=${DATE}"
ssh:
  emit_keys_to_console: false
users:
  - name: root
    lock_passwd: false
    hashed_passwd: ${PASSWD}
    ssh_authorized_keys: ${AUTHORIZED_KEYS}
