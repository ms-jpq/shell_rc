#cloud-config
---
bootcmd:
  - - cloud-init-per
    - once
    - --
    - date
    - "--set=${DATE}"
users:
  - name: root
    lock_passwd: false
    hashed_passwd: ${PASSWD}
    ssh_authorized_keys: ${AUTHORIZED_KEYS}
