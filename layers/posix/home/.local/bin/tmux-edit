#!/usr/bin/env -S -- bash -Eeuo pipefail -O dotglob -O nullglob -O extglob -O failglob -O globstar

set -o pipefail

: "${EDITOR?}"
: "${XDG_CONFIG_HOME?}"

# shellcheck disable=SC2086
exec -- "$XDG_CONFIG_HOME/tmux/libexec/open.sh" $EDITOR "$@"
