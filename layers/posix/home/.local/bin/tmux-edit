#!/usr/bin/env -S -- bash -Eeu -o pipefail -O dotglob -O nullglob -O extglob -O failglob -O globstar

set -o pipefail

# shellcheck disable=SC2154,SC2086
exec -- "$XDG_CONFIG_HOME/tmux/libexec/tmux-open.sh" $EDITOR
