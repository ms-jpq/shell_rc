---
git:
  paging:
    pager: delta --paging=never --24-bit-color=never

notARepository: quit

gui:
  scrollHeight: 1
  theme:
    activeBorderColor:
      - blue
    inactiveBorderColor:
      - black
    optionsTextColor:
      - black
    selectedLineBgColor:
      - reverse
