protocol mailto
action launch --type=os-window -- zsh -c 'aerc "$1"' _ ${URL}

protocol file
mime inode/directory
action launch --type=os-window -- zsh -c '"$HOME/.local/libexec/yazi.sh" -- "$1"' _ ${FILE_PATH}

protocol file
action launch --type=os-window -- zsh -c 'nvim -- "$1"' _ ${FILE_PATH}
