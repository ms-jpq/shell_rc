disable_checkpoint_signature = true
plugin_cache_dir             = "$XDG_CACHE_HOME/terraform/plugin"

provider_installation {
  filesystem_mirror {
    path = "$XDG_STATE_HOME/terraform/providers"
  }
}
