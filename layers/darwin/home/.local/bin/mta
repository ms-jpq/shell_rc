#!/usr/bin/env -S -- bash -Eeu -o pipefail -O dotglob -O nullglob -O extglob -O failglob -O globstar

set -o pipefail

launchctl list | cut -d $'\t' -f 3 | grep --extended-regexp --regexp '^mbsync\.' --regexp '^imap\.notify\.' --regexp '^mnotify\.' | sort
