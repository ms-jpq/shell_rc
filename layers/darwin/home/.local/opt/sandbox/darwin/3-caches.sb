(version 1)

(allow file-read* file-write*
  (subpath
    (xdg-cache "")))

(deny file-read* file-write*
  (regex
    (string-append "^" (xdg-cache "") "com\\.apple\\.")
    (string-append "^" (xdg-cache "") "nvim\\.")
    (string-append "^" (xdg-cache "aerc"))
    (string-append "^" (xdg-cache "aerc.sock"))
    (string-append "^" (xdg-cache "askpermissiond"))
    (string-append "^" (xdg-cache "CloudKit"))
    (string-append "^" (xdg-cache "containers"))
    (string-append "^" (xdg-cache "EnergyKit"))
    (string-append "^" (xdg-cache "Firefox"))
    (string-append "^" (xdg-cache "GameKit"))
    (string-append "^" (xdg-cache "GeoServices"))
    (string-append "^" (xdg-cache "icloudmailagent"))
    (string-append "^" (xdg-cache "isync"))
    (string-append "^" (xdg-cache "maildir"))
    (string-append "^" (xdg-cache "Mozilla"))
    (string-append "^" (xdg-cache "PassKit"))
    (string-append "^" (xdg-cache "SharedImageCache"))
    (string-append "^" (xdg-cache "SiriTTS"))
  ))

