bind n new-window

bind w kill-pane
bind q kill-window

bind -r = previous-window
bind -r - next-window

bind ] split-window -h
bind [ split-window -v


bind -r o previous-layout
bind -r p next-layout
