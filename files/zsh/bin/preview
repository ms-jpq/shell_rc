#!/usr/bin/env bash

set -eu
set -o pipefail


TARGET="$1"

export PAGER='tee'

if [[ -d "$TARGET" ]]
then
  exec exa --color=always --group-directories-first --icons -ahF1 "$TARGET"
else
  exec bat --color=always "$TARGET"
fi

