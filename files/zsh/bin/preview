#!/usr/bin/env bash

set -eu
set -o pipefail


TARGET="$1"

export PAGER='tee'

if [[ -d "$TARGET" ]]
then
  exa --color=always --group-directories-first --icons -ahF1 "$TARGET"
else
  bat --color=always "$TARGET"
fi

