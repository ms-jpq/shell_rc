[project]
name    = "sh"
version = "0"

dependencies = [
  "s3cmd@https://github.com/s3tools/s3cmd/archive/heads/master.tar.gz",
  "tomli-w",
  "tomli",
]

[project.optional-dependencies]
dev = ["mypy", "types-PyYAML", "types-psutil", "black", "isort"]
