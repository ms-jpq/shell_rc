#!/usr/bin/env bash

set -Eeu
set -o pipefail
shopt -s nullglob globstar


bin="$(dirname -- "$(realpath -- "$0")")"
PATH="$(paths --silent remove -- "$bin")"

export LC_ALL=C
export LANG=C

exec fzf "$@"
