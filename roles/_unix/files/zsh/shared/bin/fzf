#!/usr/bin/env bash

set -eu
set -o pipefail
shopt -s globstar
shopt -s nullglob


bin="$(dirname -- "$(realpath -- "$0")")"
PATH="$(paths --silent remove -- "$bin")"

export LC_ALL=C
export LANG=C

exec fzf "$@"
